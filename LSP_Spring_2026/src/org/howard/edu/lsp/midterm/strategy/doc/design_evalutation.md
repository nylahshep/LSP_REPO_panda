Based on the design of this class, the method uses multiple 'if' statements to handle customer types. This is not practical if modifications are needed. Bugs are at a higher risk of occuring because every customer type has a fixed formula that will requiring editing (hard coded). Additionally, there is poor extensibility if new discounts are chosen to be ran, then the method would have to be rewritten.